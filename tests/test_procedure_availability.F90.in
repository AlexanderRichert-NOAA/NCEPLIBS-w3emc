!> @file
!> @brief Test module interface procedure availability
!> @author Auto-generated test
!>
!> This test checks that all procedures identified in the source code
!> can be successfully imported from the w3emc module using "only" clauses.
!> If any procedure is not available, compilation will fail.

program test_procedure_availability
#ifdef USE_W3EMC_MODULE
#ifdef KIND_4
  use w3emc_4, only: &
#elif defined(KIND_D)
  use w3emc_d, only: &
#elif defined(KIND_8)
  use w3emc_8, only: &
#endif
    @PROC_USE_STATEMENT@
#endif
  implicit none
  
  ! This program does nothing at runtime. The actual test is the compilation
  ! itself - if any procedure in the "only" list is not available in the module,
  ! the compiler will fail. This guarantees all exported procedures exist.
  
end program test_procedure_availability
